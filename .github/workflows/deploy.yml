name: Build and Deploy

on: 
  push:
  workflow_dispatch:

jobs:
  update-infrastructure:
    runs-on: [self-hosted]
    steps:
      - name: checkout repo
        uses: actions/checkout@v2

      - name: build and deploy
        env: 
          REACT_APP_CLIENT_ID: ${{ secrets.REACT_APP_CLIENT_ID }}
          REACT_APP_MICROSOFT_TENANT: ${{ secrets.REACT_APP_MICROSOFT_TENANT }}
        run: |
          docker-compose build
          docker-compose up -d
